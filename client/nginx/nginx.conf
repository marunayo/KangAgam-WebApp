server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
    # Baris ini adalah kuncinya:
    # Jika file atau direktori tidak ditemukan, kembalikan ke index.html
    try_files $uri $uri/ /index.html;
  }
}